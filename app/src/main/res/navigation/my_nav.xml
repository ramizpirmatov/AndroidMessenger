<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/my_nav"
    app:startDestination="@id/active_chats">

    <fragment
        android:id="@+id/active_chats"
        android:name="com.example.messenger.ui.ActiveChatsFragment"
        android:label="fragment_active_chats"
        tools:layout="@layout/fragment_active_chats" />
    <fragment
        android:id="@+id/contacts"
        android:name="com.example.messenger.ui.ContactsFragment"
        android:label="fragment_contacts"
        tools:layout="@layout/fragment_contacts" />
</navigation>